;--------------------------------
@SELL_FIGHTMONEY
;--------------------------------
;死鬥場収入

;観戦したまま終わった場合チケットを減らす
IF GETBIT(FLAG:700,5)
SIF TEQUIP:55
	ITEM:35 -= 1
ENDIF

IF TFLAG:402 > 0
	IF TFLAG:402 > 12000
		TFLAG:402 = 12000
	ENDIF
	PRINTFORMW 從死鬥場的觀戰費中獲得{TFLAG:402 * 5}點收入
MONEY += TFLAG:402 * 5
EX_FLAG:4444 += TFLAG:402 * 5
TFLAG:402 = 0
ENDIF